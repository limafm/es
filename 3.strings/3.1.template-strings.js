const pl1 = 'javascript';
const pl2 = "php";
const randomNumber = 15;

const example = pl1 + ' is better than ' + pl2;

// const example = `${pl1} is batter than ${pl2}`;

// let example = `Random number: ${randomNumber}.`
// example += `\nRandom number * 10: ${randomNumber * 10}`;

console.log(example);
